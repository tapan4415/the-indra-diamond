import type {Metadata} from "next";import {headers} from "next/headers";import "./globals.css";import "./time.css";
export async function generateMetadata():Promise<Metadata>{const h=await headers(),host=h.get("host")||"localhost:3000",protocol=host.includes("localhost")?"http":"https",image=`${protocol}://${host}/og.png`;return {title:"The Indra Diamond · Case 47-ID",description:"Authorized Treasury Security Division recovery terminal for Case 47-ID.",openGraph:{title:"The Indra Diamond · Case 47-ID",description:"Authorized Treasury Security Division recovery terminal.",images:[image]},twitter:{card:"summary_large_image",title:"The Indra Diamond · Case 47-ID",description:"Authorized Treasury Security Division recovery terminal.",images:[image]}}}
export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
